import 'package:flutter/material.dart';

class WebsocketScreen extends StatelessWidget {
  const WebsocketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Websocket Screen'),
      ),
    );
  }
}